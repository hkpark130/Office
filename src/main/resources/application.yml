
spring:
  profiles:
    active: local

server:
  port: 80
  servlet:
    session:
      cookie:
        max-age: 86400 # 86400

---
constants:
  keycloak-url: "http://192.168.0.48:8080"
  client-secret: "Q0WcVVGIFjQDRH57kQ6oAeWKSvyW3Cci"
  frontend: "http://192.168.0.5:8080"
  admin: "test"
  admin-id: "5ca394e9-52f7-44ef-bae4-fafe90d63f2e"

# keycloak
spring:
  config:
    activate:
      on-profile: local
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${constants.keycloak-url}/realms/sso-test
      client:
        provider:
          keycloak:
            issuer-uri: ${constants.keycloak-url}/realms/sso-test
            user-name-attribute: preferred_username
        registration:
          keycloak:
            provider: keycloak
            client-name: back-office-api
            client-id: back-office-api
            client-secret: ${constants.client-secret}
            scope: openid,offline_access,profile
            authorization-grant-type: authorization_code
#            redirect-uri: ${constants.keycloakUrl}/login/oauth2/code/external
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
  jpa:
    show_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
      path: /h2-console

---
constants:
  keycloak-url: ${KEYCLOAK_URL}
  frontend: ${FRONTEND}
  admin: ${ADMIN}
  admin-id: ${ADMIN_ID}

spring:
  config:
    activate:
      on-profile: prod
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_URL}/realms/sso-test
      client:
        provider:
          keycloak:
            issuer-uri: ${KEYCLOAK_URL}/realms/sso-test
            user-name-attribute: preferred_username
        registration:
          keycloak:
            provider: keycloak
            client-name: back-office-api
            client-id: back-office-api
            client-secret: ${CLIENT_SECRET}
            scope: openid,offline_access,profile
            authorization-grant-type: authorization_code
  datasource:
    url: ${DB_URL}
    driverClassName: org.mariadb.jdbc.Driver
    username: ${DB_USER}
    password: ${DB_PW}
  sql:
    init:
      mode: never
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: update
